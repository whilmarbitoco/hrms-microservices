services:
  # ── RabbitMQ ───────────────────────────────────────────────────────────────
  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    networks:
      - dokploy-network
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASS}
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 15s
      timeout: 10s
      retries: 5
    labels:
      - "traefik.enable=false" # Prevents Traefik from trying to route HTTP to RabbitMQ

  # ── Auth Microservice ───────────────────────────────────────────────────────
  auth-ms:
    build:
      context: ./auth-ms
    networks:
      - dokploy-network
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      FLASK_ENV: production
      SECRET_KEY: ${SECRET_KEY}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      DATABASE_URL: ${AUTH_DB_URL}
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER}
      RABBITMQ_PASS: ${RABBITMQ_PASS}
      CORS_ORIGINS: ${CORS_ORIGINS}
      TALISMAN_FORCE_HTTPS: "false"
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=dokploy-network"
      - "traefik.http.routers.auth-router.rule=Host(`hrms-api.whilmarbitoco.qzz.io`) && PathPrefix(`/auth`)"
      - "traefik.http.routers.auth-router.entrypoints=websecure"
      - "traefik.http.routers.auth-router.tls.certresolver=letsencrypt"
      - "traefik.http.routers.auth-router.service=auth-svc"
      - "traefik.http.services.auth-svc.loadbalancer.server.port=8000"

  # ── Employee Microservice ───────────────────────────────────────────────────
  employee-ms:
    build:
      context: ./employee-ms
    networks:
      - dokploy-network
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      FLASK_ENV: production
      SECRET_KEY: ${SECRET_KEY}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      DATABASE_URL: ${EMPLOYEE_DB_URL}
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER}
      RABBITMQ_PASS: ${RABBITMQ_PASS}
      CORS_ORIGINS: ${CORS_ORIGINS}
      TALISMAN_FORCE_HTTPS: "false"
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=dokploy-network"
      - "traefik.http.routers.employee-router.rule=Host(`hrms-api.whilmarbitoco.qzz.io`) && (PathPrefix(`/employees`) || PathPrefix(`/departments`) || PathPrefix(`/employee-roles`))"
      - "traefik.http.routers.employee-router.entrypoints=websecure"
      - "traefik.http.routers.employee-router.tls.certresolver=letsencrypt"
      - "traefik.http.routers.employee-router.service=employee-svc"
      - "traefik.http.services.employee-svc.loadbalancer.server.port=8000"

  # ── Payroll Microservice ────────────────────────────────────────────────────
  payroll-ms:
    build:
      context: ./payroll-ms
    networks:
      - dokploy-network
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      FLASK_ENV: production
      SECRET_KEY: ${SECRET_KEY}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      DATABASE_URL: ${PAYROLL_DB_URL}
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER}
      RABBITMQ_PASS: ${RABBITMQ_PASS}
      CORS_ORIGINS: ${CORS_ORIGINS}
      TALISMAN_FORCE_HTTPS: "false"
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=dokploy-network"
      - "traefik.http.routers.payroll-router.rule=Host(`hrms-api.whilmarbitoco.qzz.io`) && PathPrefix(`/payroll`)"
      - "traefik.http.routers.payroll-router.entrypoints=websecure"
      - "traefik.http.routers.payroll-router.tls.certresolver=letsencrypt"
      - "traefik.http.routers.payroll-router.service=payroll-svc"
      - "traefik.http.services.payroll-svc.loadbalancer.server.port=8000"

  # ── Leave Microservice ──────────────────────────────────────────────────────
  leave-ms:
    build:
      context: ./leave-ms
    networks:
      - dokploy-network
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      FLASK_ENV: production
      SECRET_KEY: ${SECRET_KEY}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      DATABASE_URL: ${LEAVE_DB_URL}
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER}
      RABBITMQ_PASS: ${RABBITMQ_PASS}
      CORS_ORIGINS: ${CORS_ORIGINS}
      TALISMAN_FORCE_HTTPS: "false"
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=dokploy-network"
      - "traefik.http.routers.leave-router.rule=Host(`hrms-api.whilmarbitoco.qzz.io`) && PathPrefix(`/leave`)"
      - "traefik.http.routers.leave-router.entrypoints=websecure"
      - "traefik.http.routers.leave-router.tls.certresolver=letsencrypt"
      - "traefik.http.routers.leave-router.service=leave-svc"
      - "traefik.http.services.leave-svc.loadbalancer.server.port=8000"

volumes:
  rabbitmq_data:

networks:
  dokploy-network:
    external: true
